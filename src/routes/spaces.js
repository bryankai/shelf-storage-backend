const express = require('express')

const router = express.Router()
const hostsController = require('../controllers/spaces')

////////////////////////////////////////////////////////////////////
// Spaces
////////////////////////////////////////////////////////////////////

// Public viewing (no authorization needed)

// Need to test

router.get('/', hostsController.getAllSpaces)
router.get('/:id/', hostsController.getOneSpace)

////////////////////////////////////////////////////////////////////
// Export
////////////////////////////////////////////////////////////////////

module.exports = router
